const arr = [1,2,3];

arr.forEach(console.log)

for(let i = 0 ; i < arr.length ; i++) {
    console.log(arr[i]);
}

// java, html, css, js. SQL(oracle DB), 

// 라이브러리 : survlet/JSP, jQuery
// spring framework

10 + 20
var a = 10;
a;